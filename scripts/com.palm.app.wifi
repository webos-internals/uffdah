#!/bin/sh

APPID=`basename $0`
APPNAME="Wifi Profiles"
APPDIR="/var/preferences/com.palm.wifi/"
APPNOTE="Device Restart required after restore"

source `dirname $0`/srf.app.info

FILES="prefsDB.sl"
OPTIONAL=true

source `dirname $0`/srf.app.files

exit 0
